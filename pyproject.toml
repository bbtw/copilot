[project]
name = "lang-graph-state"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=0.2",
    "cvxpy>=1.5",
    "numpy>=1.26",
    "openai>=2.33.0",
    "langsmith>=0.1",
    "dotenv>=0.9.9",
]

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8.0"]
