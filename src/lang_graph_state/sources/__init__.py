from lang_graph_state.sources import customer_profile, insights, monte_carlo, optimizer

ALL_SOURCES = (
    customer_profile.definition,
    insights.definition,
    optimizer.definition,
    monte_carlo.definition,
)
