LLM_GATEWAY_BASE_URL=http://localhost:11434/v1
LLM_MODEL_ID=llama3.2
# LLM_MODEL_PROVIDER=bedrock          # set in production (enterprise gateway only)
# LLM_GATEWAY_API_KEY=your_key_here   # set in production if the gateway requires one
